#include <cstdlib>
#include <string>
#include <iostream>
#include <vector>

using namespace std;

int minDeletions(string s){
    // make a 2d frquency array
    // sort arry based on frequency
    // go from largest to smallest, if duplicate freq count dist to next smallest by using
        // vector<int[2]> freq;
        vector<int[2]> freq;
    for(char c : s){

    }

    return 0;
}

int main(){
    string s = "   000020130 aristeni";
    cout << minDeletions(s) << endl;
    return 0;
}
