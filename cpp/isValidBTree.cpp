#include <cstdlib>
#include <unordered_map>
#include <cassert>
#include "helperFunctions.h"

using namespace std;

bool isValidBST(TreeNode* root) {
    if (root == nullptr) {
        if (root->left != nullptr ) {
            statements
        }
    } else {
    }

}

int main(){
    return 0;
}
