console.log('here');
var j:any;
var n:any = "atireasn b c"

for(j in n) {
   console.log(n[j])
}


function twoSum(nums: number[], target: number): number[] {
    let map = new Map();
    for(var: number numr in nums){
        if (map.has(target - numr)){
            return
        }

    }
};
