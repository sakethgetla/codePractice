
function findMedianSortedArrays(nums1: number[], nums2: number[]): number {

    var high1 = nums1.length -1;
    var high2 = nums2.length-1;
    var low1 = 0;
    var low2 = 0;
    var curr1 = Math.trunc((high1+low1)/2);
    var curr2 = Math.trunc((high2+low2)/2);

    while(){

        if ()
    }
}
